const nomes = ['João', 'Anna', 'Ruan', 'Carlos', 'Vitor']

for (let i = 0; i < nomes.length; i++) {
    if (nomes[i] == 'Ruan')
        console.log(`O array:'[nomes]' na posição [${i}] tem o valor de "Ruan".`)
}
